// FlashListExample - How to use FlashList for smooth scrolling
// This is a reference component - not actively used in the app

// Note: The @shopify/flash-list types may vary by version.
// For production use, ensure types match your installed version.

/*
import { FlashList } from '@shopify/flash-list';

interface ListItem {
  id: string;
  title: string;
  subtitle: string;
}

export default function FlashListExample({
  data,
  renderItem,
}: {
  data: ListItem[];
  renderItem: ({ item }: { item: ListItem }) => React.ReactElement;
}) {
  return (
    <FlashList
      data={data}
      renderItem={renderItem}
      estimatedItemSize={80}
      keyExtractor={(item) => item.id}
    />
  );
}
*/
