# ScholarTrack

> Student Transport Safety App

## GSD: Grab → Do → Done

```bash
# Quick start
cd ~/ScholarTrack-Expo54
npm install --legacy-peer-deps
npm start
```

## Do

| Task | Do |
|------|----|
| Fix bug | Find → Fix → `npm test` → Commit |
| New feature | Build → Test → Commit |
| Check | `npx tsc --noEmit` |

## Rules
- Test before commit
- No secrets in code
- Accessibility (user is 60)

## Memory
- `memory/YYYY-MM-DD.md`
- `MEMORY.md`

## Stack
- Expo/React Native + Supabase
